# P003-T003 — Add explicit status-update steps to SKILL.md

**Status:** Verified
**Owner:** Implementer
**Plan:** P003

## Objective

Update `SKILL.md` Planner and Implementer procedures to instruct status advancement, aligned with the new `SPEC.md` language: the Implementer marks the Task `In Progress` when starting and `Implemented` when work and evidence are complete; the Planner marks the Plan `Planned` when ready for implementation and `Completed` only when all required Tasks are `Verified`.

**Depends on:**

- P003-T002

## Definition of Done

- [x] Implementer Procedure instructs marking the Task `In Progress` when starting implementation.
- [x] Implementer Procedure instructs marking the Task `Implemented` when the work and evidence are complete.
- [x] Planner Procedure instructs marking the Plan `Planned` when it is ready for implementation.
- [x] Planner Procedure instructs marking the Plan `Completed` only when all required Tasks are `Verified`.
- [x] `SKILL.md` stays aligned with `SPEC.md` and conformant with the Agent Skills spec (valid `name`, `description`, `license`, and `metadata` frontmatter).

## Implementation

Updated `SKILL.md` procedures aligned with the new `SPEC.md` language: the Planner Procedure now instructs marking the Plan `Planned` when ready for implementation; the Implementer Procedure now instructs marking the Task `In Progress` when beginning implementation and `Implemented` when the work and evidence are complete. Frontmatter (`name`, `description`, `license`, `metadata`) is unchanged and valid.

## Implementation Evidence

- Commit: `19104b5`
- Test: `python3 tools/validate.py --root .`
- Result: passed

## Verification

**Result:** Verified

**By:** planner-agent
**Date:** 2026-09-14

SKILL.md Planner Procedure instructs marking the Plan `Planned` (step 6) and `Completed` only when all required Tasks are `Verified` (step 9); Implementer Procedure instructs `In Progress` (step 5) and `Implemented` (step 7). Frontmatter remains valid per the Agent Skills spec.