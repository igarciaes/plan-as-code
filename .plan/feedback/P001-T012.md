# Feedback — P001-T012

**Subject:** P001-T012 — Implement conformance fixtures

## P001-T012-FB001 — Implementation evidence

**Author:** Implementer
**Date:** 2026-09-13

Implemented conformance fixtures (T012).

**Commit:** `77135c9`

**Changed:**

- `conformance/valid/core/`, `agent/`, `verified/`, `automated/` — valid fixtures for each conformance level, including feedback threads and conformance declarations where applicable.
- `conformance/invalid/duplicate-id/`, `missing-reference/`, `cyclic-dependency/`, `invalid-state/`, `missing-verification/`, `invalid-feedback/`, `invalid-ownership/` — invalid fixtures for each core invariant.
- `tools/validate.py` — added `STATE` well-formedness check and global acceptance-criterion ownership check (INV-004).
- `tests/test_fixtures.py` — automated tests asserting valid fixtures pass and each invalid fixture reports its expected violation.

**Test command:**

```text
python3 -m unittest discover -s tests
```

**Result:** 16 passed, 0 failed.

**Validation:**

```text
python3 tools/validate.py --root conformance/valid/<level>
```

Result: OK for all four valid levels; each invalid fixture reports its targeted invariant violation.