# Feedback — P001-T011

**Subject:** P001-T011 — Implement machine-checkable protocol invariants

## P001-T011-FB001 — Implementation evidence

**Author:** Implementer
**Date:** 2026-09-13

Implemented machine-checkable protocol invariants (T011).

**Commits:**

- `2a9c0e8` — SPEC Section 28 "Protocol invariants" (INV-001..INV-012) with checkability rules.
- `5026b05` — `tools/validate.py` (dependency-free validator) and `tests/test_invariants.py` (14 automated tests).

**Changed:**

- `SPEC.md` — new Section 28 defining INV-001..INV-012.
- `tools/validate.py` — new dependency-free validator: parses the normalized model from plan records and feedback threads, checks all invariants, supports `--root`, `--check`, `--min-verification`, and `--json`; reads `**Verification minimum:**` from `.plan/README.md`.
- `tests/test_invariants.py` — new tests covering every invariant (valid + violating fixtures).

**Test command:**

```text
python3 -m unittest discover -s tests
```

**Result:** 14 passed, 0 failed.

**Validation:**

```text
python3 tools/validate.py --root .
```

Result: OK — no protocol invariant violations found.