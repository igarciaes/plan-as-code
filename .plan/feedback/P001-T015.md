# Feedback — P001-T015

**Subject:** P001-T015 — Update agent and repository documentation

## P001-T015-FB001 — Implementation evidence

**Author:** Implementer
**Date:** 2026-09-13

Updated agent and repository documentation (T015).

**Commit:** `ab2391a`

**Changed:**

- `README.md` — v0.2.0 references; revised workflow diagram; protocol-semantics overview (normalized model, state derivation, conformance levels, invariants); verification-independence explanation; repository contents updated for `conformance/`, `tools/`, `tests/`.
- `AGENTS.md` — artifact table updated for v0.2.0 plus `conformance/`, `tools/`, `tests/`; §29.1 projection reference; build/validation commands (`python3 tools/validate.py --root .`, `python3 -m unittest discover -s tests`).
- `SKILL.md` — frontmatter version bumped to 0.2.0; revised workflow diagram; note that task lists contain implementation tasks; decision rules for state derivation, verification independence, and invariant validation.
- `.plan/README.md` — acceptance-criterion, decision, and finding ID formats; conformance declaration and verification-minimum instructions.
- `CHANGELOG.md` — `[Unreleased]` entry for the v0.2.0 changes.
- `tools/validate.py` — `load_repo_config` now ignores fenced code blocks so documentation examples do not set the verification minimum.

**Test command:**

```text
python3 -m unittest discover -s tests
```

**Result:** 22 passed, 0 failed.

**Validation:**

```text
python3 tools/validate.py --root .
```

Result: OK.