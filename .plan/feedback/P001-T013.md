# Feedback — P001-T013

**Subject:** P001-T013 — Update PaC schemas

## P001-T013-FB001 — Implementation evidence

**Author:** Implementer
**Date:** 2026-09-13

Updated PaC schemas (T013).

**Commit:** `7b25e26`

**Changed:**

- `schemas/plan.schema.json` — represents the revised model: acceptance criteria as `{id, text}` with `AC-P###-T###-NN` patterns, plan-level findings with the finding lifecycle enum, planning decisions (`P###-D###`) with context/decision/rationale/affected/resulting changes, verification outcomes with `independence_level` (0–4) and typed evidence references, and an optional conformance declaration.
- `schemas/feedback.schema.json` — append-only semantics documented; projection reference updated to Section 29.1.
- `tests/test_schemas.py` — automated tests asserting schema validity and representation of the revised entities.

**Test command:**

```text
python3 -m unittest discover -s tests
```

**Result:** 22 passed, 0 failed.

**Validation:**

```text
python3 -c "import json; [json.load(open(f)) for f in ['schemas/plan.schema.json','schemas/feedback.schema.json']]"
```

Result: valid JSON.