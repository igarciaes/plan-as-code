# P003-T004 — Update examples for explicit status transitions

**Status:** Implemented
**Owner:** Implementer
**Plan:** P003

## Objective

Update `examples/` so Plan and Task records demonstrate the explicit transition ownership: Plan statuses are `Planned` or `Completed` per SPEC §8, and Task statuses are `In Progress` or `Implemented` per SPEC §9, without contradicting `SPEC.md`, `SKILL.md`, or the schema projection (SPEC §20.1).

**Depends on:**

- P003-T003

## Definition of Done

- [ ] Plan examples show `Planned` and `Completed` statuses consistent with SPEC §8.
- [ ] Task examples show `In Progress` and `Implemented` statuses consistent with SPEC §9.
- [ ] Examples validate against `schemas/plan.schema.json` and `schemas/task.schema.json` via the §20.1 projection where applicable.
- [ ] Examples do not contradict `SPEC.md` or `SKILL.md`.

## Implementation

Updated `examples/` to demonstrate the explicit transition ownership: `P001.md` remains `Planned` and `P002.md` remains `Completed` (SPEC §8); `P001-T001.md` is now `Implemented` and `P001-T002.md` is now `In Progress` (SPEC §9), respecting the P001-T002 → P001-T001 dependency. `P002-T001.md` stays `Verified` under the completed `P002` plan. Statuses remain within the schema vocabularies per the §20.1 projection.

## Implementation Evidence

- Commit: `19104b5`
- Test: `python3 -m unittest discover -s tests`
- Result: passed

## Verification

**Result:** Pending

**By:** planner-agent
**Date:** 2026-09-14