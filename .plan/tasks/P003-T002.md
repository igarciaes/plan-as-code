# P003-T002 — Make Task status transitions explicit in SPEC.md §9

**Status:** Verified
**Owner:** Implementer
**Plan:** P003

## Objective

Add normative statements to `SPEC.md` §9 stating that the Implementer owns the Task's implementation lifecycle state and MUST mark the Task `In Progress` when beginning implementation and `Implemented` when the implementation and its evidence are complete, while preserving the §9.5 reason rule and the §9.6 Planner-only `Verified` rule.

**Depends on:**

- P003-T001

## Definition of Done

- [x] §9 states that the Implementer owns the Task's implementation lifecycle state (`In Progress`, `Implemented`).
- [x] §9.3 states the Implementer MUST mark the Task `In Progress` when beginning implementation.
- [x] §9.4 states the Implementer MUST mark the Task `Implemented` when the implementation and its evidence are complete.
- [x] §9.5 requires the reason for a failed verification to be recorded; preserved.
- [x] §9.6 "Only the Planner MAY transition a Task to `Verified`" is preserved.
- [x] §11 and §13.2 "Task implementation lifecycle state" ownership is consistent with the new language.

## Implementation

Added normative statements to `SPEC.md` §9: an ownership paragraph stating the Implementer owns the Task's implementation lifecycle state (`In Progress`, `Implemented`) and only the Planner MAY transition a Task to `Verified`; an `In Progress` transition requirement in §9.3; and an `Implemented` transition requirement in §9.4. §9.5 (reason rule) and §9.6 (Planner-only `Verified`) are preserved, and §11/§13.2 already state Implementer ownership of the Task's implementation lifecycle state.

## Implementation Evidence

- Commit: `19104b5`
- Test: `python3 tools/validate.py --root .`
- Result: passed

## Verification

**Result:** Verified

**By:** planner-agent
**Date:** 2026-09-14

SPEC.md §9 states the Implementer owns the Task's implementation lifecycle state and MUST advance it (`In Progress`, `Implemented`); §9.5 (reason rule) and §9.6 (Planner-only `Verified`) are preserved; §11 and §13.2 are consistent.