# P003-T001 — Make Plan status transitions explicit in SPEC.md §8

**Status:** Verified
**Owner:** Implementer
**Plan:** P003

## Objective

Add normative statements to `SPEC.md` §8 stating that the Planner owns the Plan status and MUST mark the Plan `Planned` when it is ready for implementation and `Completed` when all required Tasks are `Verified`, while preserving the existing §8.3 `Completed` constraint and §8.4 `Cancelled`.

**Depends on:**

- None

## Definition of Done

- [x] §8 states that the Planner owns the Plan status.
- [x] §8.2 states the Planner MUST mark the Plan `Planned` when it is ready for implementation.
- [x] §8.3 states the Planner MUST mark the Plan `Completed` when all required Tasks are `Verified`.
- [x] The existing §8.3 rule that a Plan MUST NOT be marked `Completed` while any required Task is not `Verified` is preserved.
- [x] The language is normative (MUST/SHOULD per RFC 2119).

## Implementation

Added normative statements to `SPEC.md` §8: an ownership paragraph stating the Planner owns the Plan status and MUST advance it, a `Planned` transition requirement in §8.2, and a `Completed` transition requirement in §8.3 while preserving the existing "MUST NOT be marked `Completed`" rule.

## Implementation Evidence

- Commit: `19104b5`
- Test: `python3 tools/validate.py --root .`
- Result: passed

## Verification

**Result:** Verified

**By:** planner-agent
**Date:** 2026-09-14

SPEC.md §8 states the Planner owns the Plan status and MUST advance it (`Planned`, `Completed`); §8.3 preserves the "MUST NOT be marked `Completed` while any required Task is not `Verified`" rule; language is normative per RFC 2119.