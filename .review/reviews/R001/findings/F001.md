# F001 — SPEC Section 30 subsection misnumbered

Status: closed

Severity: low

Location:
SPEC.md (line 1311)

## Observation

The `## 30. Agent interoperability` section contains a subsection numbered `### 21.1 Discovery`; it should be `### 30.1 Discovery`. The duplicate number collides with `### 21.1 Compatibility path` in Section 21.

## Recommendation

Rename the heading to `### 30.1 Discovery`.

## Decision

Status: accepted

Rationale:
The heading will be renamed to `### 30.1 Discovery` to remove the duplicate subsection number.

## Verification

Status: verified

Evidence:
- Commit `6bbee78` renamed the subsection heading in `SPEC.md` from `### 21.1 Discovery` to `### 30.1 Discovery`.
- `SPEC.md:1326` contains `### 30.1 Discovery`; no duplicate `### 21.1` subsection remains (only `### 21.1 Compatibility path` in Section 21, unchanged).

Checks performed:
- `git log -1 -S "30.1 Discovery" -- SPEC.md` → `6bbee78 feat: machine-check invalid relationship combinations (INV-013)`.
- `grep -n "21.1\|30.1" SPEC.md`.