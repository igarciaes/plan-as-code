# P002-T001 — Restore password authentication

**Status:** Verified
**Owner:** Implementer
**Plan:** P002

## Objective

Investigate the regression reported during verification, restore password authentication, and add a regression test.

**Depends on:**

- None

## Definition of Done

- [x] Password authentication succeeds.
- [x] OAuth authentication still succeeds.
- [x] A regression test covers the failure scenario.

## Implementation

The OAuth middleware short-circuited the password path. The middleware now falls through to the password handler when no OAuth session is present, and a regression test covers the failure scenario.

## Implementation Evidence

- Commit: `9a1b2c3`
- Test: `npm test -- auth`
- Result: passed

## Verification

**Result:** Verified

**By:** planner-agent

**Date:** 2026-09-14

The implementation satisfies the Definition of Done: password authentication succeeds, OAuth still succeeds, and a regression test covers the failure scenario. An earlier review found the regression test missing; the Task returned to `In Progress`, the test was added, and verification succeeded on re-review.