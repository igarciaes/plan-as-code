# P001 — Add OAuth Authentication

**Status:** Active
**Scope:** `src/auth/`, `tests/auth/`
**Planner:** planner-agent
**Created:** 2026-09-13
**PaC version:** v0.1.0
**Current iteration:** 1

## Objective

Add OAuth 2.0 authentication so users can sign in with an external identity provider while existing password authentication continues to work.

## Scope

Describe included and excluded artifacts.

### Included

- `src/auth/provider.ts` — provider abstraction.
- `src/auth/oauth.ts` — OAuth flow.
- `tests/auth/` — provider and OAuth tests.

### Excluded

- Password authentication redesign.
- Multi-provider support (see Iteration 2 feedback decision).

## Constraints

- Existing authentication MUST remain functional.
- The provider abstraction SHOULD support future providers without breaking changes.
- No third-party authentication service is required.

## Tasks

### P001-T001 — Create OAuth provider abstraction

**Status:** Verified
**Owner:** Implementer

Define a provider abstraction that isolates OAuth details behind a stable interface, supporting one initial provider (the OAuth flow in P001-T002).

**Depends on:**

- None

#### Acceptance

- [ ] A provider interface exists.
- [ ] The interface supports one provider initially.
- [ ] The interface is tested.
- [ ] Existing authentication remains functional.

### P001-T002 — Wire OAuth authentication into the login flow

**Status:** Implemented
**Owner:** Implementer

Connect the OAuth provider abstraction to the login flow so users can authenticate with an external identity provider.

**Depends on:**

- P001-T001

#### Acceptance

- [ ] OAuth authentication succeeds.
- [ ] Authentication failures are handled.
- [ ] Existing password authentication remains functional.

---

## Findings

No findings.

---

## Verification

### P001-T001

**Status:** Verified

**Verifier:** verifier-agent

#### Evidence

- Commit: `abc1234`
- Test: `npm test -- auth/provider`
- Result: passed

#### Acceptance

- [x] A provider interface exists.
- [x] The interface supports one provider initially.
- [x] The interface is tested.
- [x] Existing authentication remains functional.

### P001-T002

**Status:** Implemented

**Verifier:** verifier-agent

#### Evidence

- Commit: `def5678`
- Test: `npm test -- auth/oauth`
- Result: pending

#### Acceptance

- [ ] OAuth authentication succeeds.
- [ ] Authentication failures are handled.
- [ ] Existing password authentication remains functional.

---

## Iterations

### Iteration 1 — Initial Plan

Initial task definition for the OAuth feature.

See also the clarification and decision in the [P001-T001 feedback thread](P001-T001.md).